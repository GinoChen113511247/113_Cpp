#include <iostream>
using namespace std;
int main()
{
    int a, b, c, d, n;
    cin >> a >> b >> c >> d >> n;
    if (a, b, c >= 15 && a, b, c <=45 )
    {
      if (d==92)
        cout << n*a; 
        
      if (d==95)
        cout << n*b; 
       
      if (d==98)
        cout << n*c; 
    }
    return 0;
}